[project]
name = "picoharness"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "pytrends-modern",
    "jsonpointer",
    "feedparser",
    "playwright",
    "beautifulsoup4",
    "tqdm",
    "lmstudio",
    "requests",
    "typing-extensions>=4.15.0",
]
